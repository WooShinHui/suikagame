import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.waterMelon',
  appName: 'waterMelon',
  webDir: 'dist'
};

export default config;
